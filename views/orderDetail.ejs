<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Payment Successful</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .topbar{background:#fff;border-bottom:1px solid #eee;height:100px}
    .inner{max-width:1226px;margin:0 auto;padding:0 20px;display:flex;align-items:center;justify-content:space-between;color:#333;height:100%}
    .logo{width:200px;height:100px;background:url('/image/logo_top.jpg.png') no-repeat center center;background-size:contain}
    .section{max-width:1226px;margin:20px auto;padding:20px;background:#fff;border-radius:12px;box-shadow:0 8px 32px rgba(0,0,0,.08)}
    .success{display:flex;align-items:center;gap:12px;font-size:20px;font-weight:700;color:#2ecc71}
    .order-info{margin-top:12px;color:#666}
    .summary{margin-top:20px;border-top:1px solid #f0f0f0;padding-top:12px}
    .toggle{display:flex;align-items:center;gap:8px;color:#333;cursor:pointer}
    .items{margin-top:12px;display:none}
    .items.show{display:block}
    .item{display:flex;justify-content:space-between;padding:10px 0;border-bottom:1px solid #f5f5f5}
    .item:last-child{border-bottom:none}
    .name{color:#333}
    .price{color:#e74c3c;font-weight:700}
  </style>
</head>
<body>
  <div class="topbar"><div class="inner"><a href="/homepage"><div class="logo"></div></a><a href="/homepage" style="color:#333;text-decoration:none">Back to Home</a></div></div>
  <div class="section">
    <div class="success">Payment Successful</div>
    <div class="order-info">Order ID: <%= order.id %>  Amount: ¥<%= Number(order.total_amount).toFixed(2) %>  Time: <%= order.created_at %></div>
    <div class="summary">
      <div class="toggle" id="toggleItems"><span>Items</span><span id="toggleText">Expand</span></div>
      <div class="items" id="itemsList">
        <% (items || []).forEach(function(it){ %>
          <div class="item">
            <div class="name"><%= it.name %> × <%= it.quantity %></div>
            <div class="price">¥<%= (it.price * it.quantity).toFixed(2) %></div>
          </div>
        <% }) %>
      </div>
    </div>
  </div>
  <script>
    (function(){
      var t=document.getElementById('toggleItems');
      var l=document.getElementById('itemsList');
      var s=document.getElementById('toggleText');
      t&&t.addEventListener('click',function(){
        var show=l.classList.toggle('show');
        s.textContent=show?'Collapse':'Expand';
      });
    })();
  </script>
</body>
</html>
