<!DOCTYPE html>
<html lang="zh-CN">

<head>
  <meta charset="UTF-8">
  <title>
    <%= product.name %> - StarShopping
  </title>
  <link rel="stylesheet" type="text/css" href="/css/style.css">
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      padding: 0;
      font-family: "Helvetica Neue", Helvetica, Arial, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
      font-size: 14px;
      color: #333;
      line-height: 1.6;
      background: #f8f9fa;
    }

    header,
    footer {
      padding: 10px 0;
      background: #fff;
      font-size: 12px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    .w {
      width: 1200px;
      margin: 0 auto;
      box-sizing: border-box;
    }

    .top {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .top ul {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
    }

    .top ul li {
      margin: 0 8px;
    }

    .top ul li a {
      text-decoration: none;
      color: #333;
      transition: color 0.3s ease;
    }

    .top ul li a:hover {
      color: #f60;
    }



    /* 返回按钮（置于 logo 下方） */
    .back-button {
      position: static;
      background: #f60;
      color: #fff;
      border: none;
      padding: 12px 20px;
      border-radius: 25px;
      cursor: pointer;
      font-size: 14px;
      font-weight: 500;
      box-shadow: 0 4px 12px rgba(255, 102, 0, 0.3);
      transition: all 0.3s ease;
      margin-top: 8px;
      display: inline-block;
    }

    .back-button:hover {
      background: #e65c00;
      transform: translateY(-2px);
      box-shadow: 0 6px 16px rgba(255, 102, 0, 0.4);
    }

    /* 购物车动画和弹窗 */
    .cart-notification {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%) scale(0);
      background: #fff;
      border-radius: 16px;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
      padding: 40px;
      text-align: center;
      z-index: 10000;
      opacity: 0;
      transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
      max-width: 400px;
      width: 90%;
    }

    .cart-notification.show {
      opacity: 1;
      transform: translate(-50%, -50%) scale(1);
    }

    .cart-notification .icon {
      width: 80px;
      height: 80px;
      background: linear-gradient(135deg, #4CAF50, #45a049);
      border-radius: 50%;
      margin: 0 auto 20px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 40px;
      color: #fff;
      animation: bounce 0.6s ease;
    }

    @keyframes bounce {

      0%,
      20%,
      53%,
      80%,
      100% {
        transform: translate3d(0, 0, 0);
      }

      40%,
      43% {
        transform: translate3d(0, -20px, 0);
      }

      70% {
        transform: translate3d(0, -10px, 0);
      }

      90% {
        transform: translate3d(0, -4px, 0);
      }
    }

    .cart-notification h3 {
      margin: 0 0 10px;
      font-size: 24px;
      color: #333;
      font-weight: 600;
    }

    .cart-notification p {
      margin: 0 0 30px;
      color: #666;
      font-size: 16px;
      line-height: 1.5;
    }

    .cart-notification .buttons {
      display: flex;
      gap: 15px;
      justify-content: center;
    }

    .cart-notification button {
      padding: 12px 24px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 14px;
      font-weight: 500;
      transition: all 0.3s ease;
      min-width: 120px;
    }

    .cart-notification .btn-primary {
      background: #f60;
      color: #fff;
    }

    .cart-notification .btn-primary:hover {
      background: #e65c00;
      transform: translateY(-2px);
    }

    .cart-notification .btn-secondary {
      background: #f8f9fa;
      color: #666;
      border: 1px solid #ddd;
    }

    .cart-notification .btn-secondary:hover {
      background: #e9ecef;
      color: #333;
    }

    .overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      z-index: 9999;
      opacity: 0;
      visibility: hidden;
      transition: all 0.3s ease;
    }

    .overlay.show {
      opacity: 1;
      visibility: visible;
    }

    /* 加入购物车按钮动画 */
    .add-to-cart-btn {
      position: relative;
      overflow: hidden;
    }

    .add-to-cart-btn::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 0;
      height: 0;
      background: rgba(255, 255, 255, 0.3);
      border-radius: 50%;
      transform: translate(-50%, -50%);
      transition: width 0.6s, height 0.6s;
    }

    .add-to-cart-btn.clicked::before {
      width: 300px;
      height: 300px;
    }

    .loading-spinner {
      display: none;
      width: 20px;
      height: 20px;
      border: 2px solid #ffffff;
      border-top: 2px solid transparent;
      border-radius: 50%;
      animation: spin 1s linear infinite;
      margin-right: 8px;
    }

    @keyframes spin {
      0% {
        transform: rotate(0deg);
      }

      100% {
        transform: rotate(360deg);
      }
    }

    .add-to-cart-btn.loading::after {
      content: '';
      display: inline-block;
      width: 16px;
      height: 16px;
      border: 2px solid #ffffff;
      border-top: 2px solid transparent;
      border-radius: 50%;
      animation: spin 1s linear infinite;
      margin-left: 8px;
    }

    .add-to-cart-btn.loading {
      pointer-events: none;
    }

    /* 产品详情区 */
    .product-container {
      width: 1200px;
      margin: 80px auto 30px;
      display: flex;
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
      overflow: hidden;
      min-height: 600px;
    }

    .picture-container {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 40px;
      background: #fafafa;
    }

    .picture-container img {
      max-width: 100%;
      max-height: 500px;
      border-radius: 8px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
      transition: transform 0.3s ease;
    }

    .picture-container img:hover {
      transform: scale(1.05);
    }

    .detail-container {
      flex: 1;
      padding: 40px;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .detail-container h1 {
      font-size: 28px;
      margin-bottom: 16px;
      font-weight: 600;
      color: #222;
      line-height: 1.3;
    }

    .detail-container .description {
      margin-bottom: 24px;
      font-size: 15px;
      color: #666;
      line-height: 1.6;
      padding: 16px;
      background: #f8f9fa;
      border-radius: 8px;
      border-left: 4px solid #f60;
    }

    .detail-container .price {
      font-size: 32px;
      color: #f60;
      margin-bottom: 32px;
      font-weight: 700;
      display: flex;
      align-items: baseline;
    }

    .detail-container .price::before {
      content: '¥';
      font-size: 20px;
      margin-right: 4px;
    }

    .detail-container .actions {
      display: flex;
      gap: 16px;
      margin-top: 20px;
    }

    .detail-container .actions input {
      padding: 14px 28px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 15px;
      font-weight: 500;
      transition: all 0.3s ease;
      min-width: 140px;
    }

    .detail-container .actions input:first-child {
      background: #fff;
      color: #f60;
      border: 2px solid #f60;
    }

    .detail-container .actions input:first-child:hover {
      background: #f60;
      color: #fff;
      transform: translateY(-2px);
      box-shadow: 0 6px 16px rgba(255, 102, 0, 0.3);
    }

    .detail-container .actions input:last-child {
      background: #f60;
      color: #fff;
      border: 2px solid #f60;
    }

    .detail-container .actions input:last-child:hover {
      background: #e65c00;
      border-color: #e65c00;
      transform: translateY(-2px);
      box-shadow: 0 6px 16px rgba(230, 92, 0, 0.4);
    }

    footer {
      margin-top: 40px;
      padding: 30px 0;
      font-size: 12px;
      color: #999;
      text-align: center;
      line-height: 1.8;
      background: #fff;
      border-top: 1px solid #eee;
    }

    footer a {
      color: #999;
      text-decoration: none;
      margin: 0 5px;
      transition: color 0.3s ease;
    }

    footer a:hover {
      color: #f60;
    }

    /* 响应式设计 */
    @media (max-width: 1240px) {

      .w,
      .product-container {
        width: 95%;
        margin-left: auto;
        margin-right: auto;
      }
    }

    @media (max-width: 768px) {
      .product-container {
        flex-direction: column;
        margin: 20px auto;
      }

      .picture-container,
      .detail-container {
        padding: 20px;
      }

      .detail-container h1 {
        font-size: 24px;
      }

      .detail-container .price {
        font-size: 28px;
      }

      .detail-container .actions {
        flex-direction: column;
      }

      .nav ul {
        flex-wrap: wrap;
      }

      .nav ul li {
        margin: 5px 10px;
      }
    }

    .tb-card{background:#fff;border-radius:12px;box-shadow:0 6px 18px rgba(0,0,0,.08)}
    .product-container{gap:24px}
    .gallery{width:460px;display:flex;gap:12px;flex-direction:column}
    .ship-info{background:#fff;border:1px solid #eee;border-radius:8px;padding:12px;color:#4a4a4a;font-size:13px;line-height:1.8}
    .ship-info .row{display:flex;flex-wrap:wrap;gap:8px}
    .ship-info .row span{white-space:nowrap}
    .gallery .thumbs{width:68px;display:flex;flex-direction:column;gap:8px}
    .gallery .thumbs img{width:68px;height:68px;object-fit:cover;border:1px solid #eee;border-radius:6px;cursor:pointer}
    .gallery .main-pic{background:#fff;border:1px solid #eee;border-radius:8px;display:flex;align-items:center;justify-content:center;padding:16px;width:420px}
    .gallery .main-pic{position:relative}
    .gallery .main-pic img{max-width:100%;max-height:420px}

    .zoom-lens{position:absolute;border:2px solid rgba(255,102,0,.6);width:200px;height:200px;background:rgba(255,255,255,.35);border-radius:8px;pointer-events:none;opacity:0;visibility:hidden;transition:opacity .12s ease;will-change:transform}
    .zoom-lens.show{opacity:1;visibility:visible}
    .zoom-overlay{position:fixed;width:560px;height:560px;background:#fff;border:1px solid #eee;border-radius:8px;background-repeat:no-repeat;overflow:hidden;opacity:0;visibility:hidden;transition:opacity .12s ease;will-change:background-position;box-shadow:0 12px 32px rgba(0,0,0,.18);z-index:10000}
    .zoom-overlay.show{opacity:1;visibility:visible}

    .detail-container{padding:20px 40px}
    .tb-pricebox{background:#fffaf2;border:1px solid #ffe8cc;border-radius:8px;padding:16px;margin:12px 0}
    .tb-price{font-size:28px;color:#ff5000;font-weight:700}
    .tb-stock{font-size:12px;color:#666;margin-top:8px}

    .tb-sku-row{display:flex;align-items:center;margin:12px 0}
    .tb-sku-title{width:60px;color:#999}
    .tb-sku-options{display:flex;gap:8px}
    .tb-sku-option{padding:8px 12px;border:1px solid #ddd;border-radius:6px;cursor:pointer}
    .tb-sku-option.selected{border-color:#ff6a00;color:#ff6a00;background:#fff2e8}

    .tb-quantity{display:flex;align-items:center;gap:8px}
    .qty-btn{width:28px;height:28px;border:1px solid #ddd;border-radius:4px;background:#fff;cursor:pointer}
    #buyQty{width:50px;height:28px;text-align:center;border:1px solid #ddd;border-radius:4px}

    .tb-actions{display:flex;gap:12px;margin-top:16px}
    .buy-btn{padding:12px 24px;background:#ff6a00;color:#fff;border:none;border-radius:6px;font-size:14px;cursor:pointer;box-shadow:0 4px 12px rgba(255,106,0,.3)}
    .buy-btn:hover{background:#ff5000}
    .cart-btn{padding:12px 24px;background:#ffecec;color:#ff6a00;border:1px solid #ffb199;border-radius:6px;font-size:14px;cursor:pointer}
    .cart-btn:hover{background:#ffd7d7}

    .tb-service{display:flex;gap:16px;font-size:12px;color:#666;margin-top:10px}
    .extra-info{margin-top:16px;background:#fff;border:1px solid #eee;border-radius:8px;padding:16px;display:flex;flex-direction:column;gap:10px}
    .extra-info .info-row{display:flex;gap:12px;align-items:flex-start;font-size:13px;color:#444}
    .extra-info .info-title{min-width:48px;color:#999}
    .extra-info .info-content{flex:1}
    .image-tabs{width:1200px;margin:20px auto;background:#fff;border:1px solid #eee;border-radius:12px;box-shadow:0 6px 18px rgba(0,0,0,.08)}
    .image-tabs .tabs-nav{display:flex;gap:24px;padding:0 16px}
    .image-tabs .tab{padding:16px 0;cursor:pointer;color:#333;position:relative;font-size:16px}
    .image-tabs .tab.active{color:#ff6700;font-weight:700}
    .image-tabs .tab.active::after{content:'';position:absolute;left:0;bottom:-1px;width:100%;height:2px;background:#ff6700;border-radius:2px}
    .image-tabs .tab-content{border-top:1px solid #eee;padding:16px}
    .image-tabs .tab-panel{display:none}
    .image-tabs .tab-panel.active{display:block}
    .spec-table{width:100%;border:1px solid #eee;border-radius:8px;overflow:hidden;border-spacing:0;border-collapse:separate;background:#fff}
    .spec-table td{padding:12px 16px;border-top:1px solid #eee;font-size:14px;color:#333}
    .spec-table tr:first-child td{border-top:none}
    .spec-table td.title{width:120px;color:#666;background:#f7f9fc}
    .spec-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;background:#f7f9fc;border-radius:8px;padding:16px;margin-bottom:12px}
    .spec-card{background:#fff;border:1px solid #eee;border-radius:8px;padding:12px;text-align:center}
    .spec-card .t{font-size:16px;font-weight:700;color:#333;margin-bottom:6px}
    .spec-card .s{font-size:12px;color:#666}
  </style>
</head>

<body>
  <div class="banner_x">
    <div class="center-content">
      <div class="logo-stack" style="display:flex;flex-direction:column;align-items:flex-start;gap:8px;margin-right:12px;">
        <a href="/homepage">
          <div class="logo"></div>
        </a>
        <button class="back-button" onclick="history.back()">← 返回</button>
      </div>
      <a href="/products/all">
        <div class="ad_top"></div>
      </a>
    </div>

    <div class="search-cart-container">
      <div class="user-buttons">
        <a href="javascript:void(0)" id="logout-link" class="icon-button" title="登出">
          <svg class="icon" viewBox="0 0 24 24" width="18" height="18"><path fill="currentColor" d="M16 13v-2H7V8l-5 4 5 4v-3h9zm6-9v16c0 1.1-.9 2-2 2H8c-1.1 0-2-.9-2-2v-4h2v4h12V4H8v4H6V4c0-1.1.9-2 2-2h12c1.1 0 2 .9 2 2z"></path></svg>
          <span class="label">登出</span>
        </a>
        <a href="/profile.html" class="icon-button" title="个人中心">
          <svg class="icon" viewBox="0 0 24 24" width="18" height="18"><path fill="currentColor" d="M12 2a10 10 0 100 20 10 10 0 000-20zm0 6a3 3 0 110 6 3 3 0 010-6zm0 8c-2.67 0-8 1.34-8 4v1h16v-1c0-2.66-5.33-4-8-4z"></path></svg>
          <span class="label">个人中心</span>
        </a>
      </div>
      <div class="cart-button">
        <a href="/cart" class="cart-link">
          <svg class="cart-icon" viewBox="0 0 24 24" width="20" height="20">
            <path fill="currentColor" d="M7 18c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zM1 2v2h2l3.6 7.59-1.35 2.45c-.16.28-.25.61-.25.96 0 1.1.9 2 2 2h12v-2H7.42c-.14 0-.25-.11-.25-.25l.03-.12L8.1 13h7.45c.75 0 1.41-.41 1.75-1.03L21.7 4H5.21l-.94-2H1zm16 16c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2z"></path>
          </svg>
          <span class="cart-text">购物车</span>
        </a>
      </div>
      <div class="search" style="position: relative;">
        <form action="/products/all" method="get">
          <div class="text fl">
            <input type="text" name="keyword" class="shuru" placeholder="华为mate70pro现货开抢">
          </div>
          <div class="submit fl">
            <input type="submit" class="sousuo" value="搜索">
          </div>
          <div class="clear"></div>
        </form>
        <div class="search-results" style="position: absolute; top: 100%; left: 0; right: 0; background: white; border-right: 1px solid #ddd; border-bottom: 1px solid #ddd; border-left: 1px solid #ddd; border-top: none; max-height: 400px; overflow-y: auto; z-index: 10000; margin-top: 1px; box-shadow: rgba(0,0,0,0.1) 0 4px 6px; display: none;"></div>
        <div class="search-suggestions" style="position: absolute; top: calc(100% + 2px); left: 0; right: 0; background: white; border: 1px solid #e0e0e0; border-radius: 0 0 8px 8px; max-height: 300px; overflow-y: auto; z-index: 10001; box-shadow: rgba(0,0,0,0.15) 0 8px 24px; display: none; margin-top: 1px;"></div>
        <div class="hot-searches" style="position: absolute; top: calc(100% + 2px); left: 0; right: 0; background: white; border: 1px solid #e0e0e0; border-radius: 0 0 8px 8px; max-height: 200px; overflow-y: auto; z-index: 10002; box-shadow: rgba(0,0,0,0.15) 0 8px 24px; display: none; margin-top: 1px;"></div>
      </div>
    </div>
  </div>

  
  




  <!-- 购物车通知弹窗 -->
  <div class="overlay" id="overlay"></div>
  <div class="cart-notification" id="cartNotification">
    <div class="icon">✓</div>
    <h3>添加成功！</h3>
    <p>商品已成功添加到购物车</p>
    <div class="buttons">
      <button class="btn-secondary" onclick="closeNotification()">继续购物</button>
      <button class="btn-primary" onclick="window.location.href='/cart'">查看购物车</button>
    </div>
  </div>

  <!-- 产品详情 -->
  <div class="product-container tb-card">
    <div class="gallery">
      <div class="main-pic">
        <img id="mainImage" src="<%= (product && product.name && (product.name.includes('云海昼夜适用oppoa1手机壳') || product.name.includes('oppoA1pro手机壳'))) ? '/image/tp122.png' : (product.image || '/image/default.png') %>" alt="<%= product.name %>">
      </div>
      
    </div>
    <div class="detail-container">
      <h1><%= (product && product.name && product.name.includes('云海昼夜适用oppoa1手机壳')) ? '适用oppoA1pro手机壳A1x活力版A58新款A97全包A96外壳A95/A93硅胶防摔A72系列A11保护套A55/A56男A57女oppo5g' : product.name %></h1>
      <div class="tb-pricebox">
        <div class="tb-price">¥<%= product.price %></div>
        <% if (typeof product.stock !== 'undefined') { %>
          <div class="tb-stock"><%= product.stock > 0 ? '现货' : '缺货' %> | 库存：<%= product.stock %></div>
        <% } %>
      </div>
      <div class="tb-sku">
        <div class="tb-sku-row">
          <div class="tb-sku-title">规格</div>
          <div class="tb-sku-options">
            <span class="tb-sku-option selected">默认</span>
          </div>
        </div>
        <% if (product.id == 205) { %>
        <div class="ship-info">
          <div class="row"><span>预计3小时内发货</span><span>|</span><span>承诺48小时内发货</span><span>|</span><span>快递: 免运费</span><span>|</span><span>广东深圳 至 上海市 黄浦区</span></div>
          <div class="row"><span>7天无理由退换</span><span>|</span><span>假一赔四</span><span>|</span><span>极速退款</span></div>
          <div class="row"><span>信用卡支付</span></div>
        </div>
        <% } %>
        <% if (product.id == 204) { %>
        <div class="ship-info">
          <div class="row"><span>48小时内发货</span><span>|</span><span>卖家直送: 免运费</span><span>|</span><span>广东深圳 至 香港岛 中西区</span></div>
          <div class="row"><span>7天无理由退换</span><span>|</span><span>7天价保</span><span>|</span><span>假一赔四</span><span>|</span><span>极速退款</span></div>
          <div class="row"><span>信用卡支付</span></div>
        </div>
        <% } %>
        <% if (product.id == 201) { %>
        <div class="extra-info">
          <div class="info-row"><span class="info-title">配送</span><span class="info-content">上海 至 香港岛 中西 · 卖家直送：免运费 现货，付款后48小时内发货</span></div>
          <div class="info-row"><span class="info-title">保障</span><span class="info-content">7天无理由退货 破损包退</span></div>
          <div class="info-row"><span class="info-title">参数</span><span class="info-content">颜色分类：小米note3-白色</span></div>
        </div>
        <% } %>
        <% if (product && product.name && (product.name.includes('oppoA1pro') || product.name.includes('云海昼夜适用oppoa1手机壳'))) { %>
        <div class="ship-info">
          <div class="row"><span>48小时内发货，预计后天送达</span><span>|</span><span>快递: 免运费</span><span>|</span><span>广东深圳 至 上海市 黄浦区</span></div>
          <div class="row"><span>7天无理由退换</span><span>|</span><span>假一赔四</span><span>|</span><span>极速退款</span></div>
          <div class="row"><span>信用卡支付</span></div>
        </div>
        <% } %>
        <div class="tb-sku-row">
          <div class="tb-sku-title">数量</div>
          <div class="tb-quantity">
            <button class="qty-btn minus" type="button">-</button>
            <input type="number" id="buyQty" min="1" value="1">
            <button class="qty-btn plus" type="button">+</button>
            <% if (typeof product.stock !== 'undefined') { %>
              <span class="tb-qty-stock">库存 <%= product.stock %> 件</span>
            <% } %>
          </div>
        </div>
      </div>
      <div class="tb-actions">
        <button class="buy-btn" data-product-id="<%= product.id %>">立即购买</button>
        <button class="cart-btn add-to-cart-btn" data-product-id="<%= product.id %>">加入购物车</button>
      </div>
      <ul class="tb-service">
        <li>正品保证</li>
        <li>七天无理由</li>
        <li>极速发货</li>
      </ul>
    </div>
  </div>

  <div class="image-tabs">
    <div class="tabs-nav">
      <div class="tab active">用户评价</div>
      <div class="tab">参数信息</div>
      <div class="tab">图文详情</div>
      <div class="tab">本店推荐</div>
    </div>
    <div class="tab-content">
      <div class="tab-panel active">
        <div style="display:flex;flex-direction:column;gap:12px">
          <div style="display:flex;gap:12px;align-items:flex-start"><img src="<%= product.id == 205 ? '/image/tp115.png' : (product.id == 204 ? '/image/tp120.png' : '/image/tp110.png') %>" style="width:32px;height:32px;border-radius:50%"><div><div style="font-weight:600;color:#333"><%= product.id == 205 ? '匿名买家' : (product.id == 204 ? '若梦若初见_' : '用户A') %></div><div style="font-size:12px;color:#666"><%= product.id == 205 ? '材质手感：光滑，方方正正无毛刺 尺寸贴合：与原机符合贴合 外观设计：与原机符合贴合' : (product.id == 204 ? '手机壳很好看，图案生动立体，简约线条勾勒时尚轮廓，色彩清新如春日晨露，细节之处暗藏小心机，与手机适配度满分，爱了😍' : '质感不错，手感细腻，贴合度高。') %></div></div></div>
          <div style="display:flex;gap:12px;align-items:flex-start"><img src="<%= product.id == 205 ? '/image/tp116.png' : (product.id == 204 ? '/image/tp121.png' : '/image/tp111.png') %>" style="width:32px;height:32px;border-radius:50%"><div><div style="font-weight:600;color:#333"><%= product.id == 205 ? '小幽Orz' : (product.id == 204 ? 'Ashleygaog' : '用户B') %></div><div style="font-size:12px;color:#666"><%= product.id == 205 ? '手机壳已经收到，简单干净透亮，很喜欢。膜有点问题，客服积极解决补发了一个，赞好评。' : '价格实惠便宜，质量很好商家很靠谱，物流速度快' %></div></div></div>
        </div>
      </div>
      <div class="tab-panel">
        <% if (product.id == 201) { %>
        <table class="spec-table">
          <tbody>
            <tr>
              <td class="title">品牌</td><td>易购</td>
              <td class="title">型号</td><td>米8</td>
            </tr>
            <tr>
              <td class="title">功能</td><td>防摔</td>
              <td class="title">保护套质地</td><td>液态硅胶</td>
            </tr>
            <tr>
              <td class="title">风格</td><td>简约,ins风</td>
              <td class="title">适用手机型号</td><td>小米</td>
            </tr>
            <tr>
              <td class="title">型号</td><td>M8</td>
              <td class="title">颜色分类</td><td>小米8红色,小米8粉色,小米8黑色,小米8白色,小米8屏幕指纹版</td>
            </tr>
            <tr>
              <td class="title">款式</td><td>保护壳</td>
              <td class="title">流行元素</td><td>纯色</td>
            </tr>
          </tbody>
        </table>
        <% } else if (product.id == 205) { %>
        <div class="spec-grid">
          <div class="spec-card"><div class="t">含环保材料</div><div class="s">是否使用环保材质</div></div>
          <div class="spec-card"><div class="t">TPU</div><div class="s">保护套质地</div></div>
          <div class="spec-card"><div class="t">简约</div><div class="s">流行元素</div></div>
          <div class="spec-card"><div class="t">简约</div><div class="s">风格</div></div>
          <div class="spec-card"><div class="t">深圳市捷凯德科技有限公司</div><div class="s">生产企业</div></div>
        </div>
        <table class="spec-table">
          <tbody>
            <tr>
              <td class="title">是否可降解</td><td>否</td>
              <td class="title">品牌</td><td>Jokod/卓酷</td>
            </tr>
            <tr>
              <td class="title">型号</td><td>reno系列透明B2</td>
              <td class="title">适用手机型号</td><td>OPPO</td>
            </tr>
            <tr>
              <td class="title">适配机型</td><td colspan="3">OPPO Reno12 Pro、OPPO Reno7、OPPO Reno7 SE、OPPO Reno9 Pro+ 等</td>
            </tr>
            <tr>
              <td class="title">颜色分类</td><td colspan="3">高透魔方【裸壳】 高透魔方【送钢化膜】 高透魔方【送水凝膜】</td>
            </tr>
          </tbody>
        </table>
        <% } else if (product.id == 204) { %>
        <div class="spec-grid">
          <div class="spec-card"><div class="t">含环保材料</div><div class="s">是否使用环保材质</div></div>
          <div class="spec-card"><div class="t">卡通图案</div><div class="s">流行元素</div></div>
          <div class="spec-card"><div class="t">保护壳</div><div class="s">款式</div></div>
          <div class="spec-card"><div class="t">简约</div><div class="s">风格</div></div>
          <div class="spec-card"><div class="t">中国制造</div><div class="s">生产企业</div></div>
          <div class="spec-card"><div class="t">是</div><div class="s">是否可降解</div></div>
        </div>
        <table class="spec-table">
          <tbody>
            <tr>
              <td class="title">品牌</td><td>匠轩</td>
              <td class="title">型号</td><td>适用vivo S30-Martin Schumm DVM</td>
            </tr>
            <tr>
              <td class="title">适用手机型号</td><td>vivo</td>
              <td class="title">型号</td><td>vivo S30</td>
            </tr>
            <tr>
              <td class="title">适配机型</td><td colspan="3">vivo S19 Pro、vivo S9e、vivo S16e、vivo S20、vivo S10、vivo S30 Pro mini、vivo S19、vivo S17 等</td>
            </tr>
            <tr>
              <td class="title">颜色分类</td><td colspan="3">新品福利★全店第二件0元（NM16590渐变鲸鱼黄） 硅胶软壳★全包镜头★不顶膜（NM1658…</td>
            </tr>
          </tbody>
        </table>
        <% } else { %>
        <div class="extra-info" style="margin-top:0">
          <div class="info-row"><span class="info-title">配送</span><span class="info-content">上海 至 香港岛 中西 · 卖家直送：免运费 现货，付款后48小时内发货</span></div>
          <div class="info-row"><span class="info-title">保障</span><span class="info-content">7天无理由退货 破损包退</span></div>
          <div class="info-row"><span class="info-title">参数</span><span class="info-content">颜色分类：小米note3-白色</span></div>
        </div>
        <% } %>
      </div>
      <div class="tab-panel">
        <% if (product.id == 205) { %>
        <div style="display:flex;gap:12px;flex-wrap:wrap"><img src="/image/tp113.png" style="width:200px;border-radius:8px;box-shadow:0 8px 24px rgba(0,0,0,.12)"><img src="/image/tp114.png" style="width:200px;border-radius:8px;box-shadow:0 8px 24px rgba(0,0,0,.12)"></div>
        <div class="buy-tip" style="margin-top:16px;border:2px solid #ff4d4f;border-radius:12px;padding:16px;background:#fff7f7;color:#d4380d;line-height:1.8;font-size:14px">
          <div style="font-weight:700;font-size:18px;text-align:center;margin-bottom:8px">亲！看过来！</div>
          <p>温馨提示：下单前请您先找到设置、关于本机核实好您的手机型号，然后按照您对应的型号下单。</p>
          <p>所有图片只是按单一型号作为效果展示图仅供参考，以收到的实物为准。发货都是按您拍下的型号颜色孔位匹配发货，请您放心购买！</p>
          <p>手机屏幕为直面屏的送钢化膜，曲面屏的送水凝膜。</p>
        </div>
        <% } else if (product.id == 204) { %>
        <div style="display:flex;gap:12px;flex-wrap:wrap"><img src="/image/tp118.png" style="width:200px;border-radius:8px;box-shadow:0 8px 24px rgba(0,0,0,.12)"><img src="/image/tp119.png" style="width:200px;border-radius:8px;box-shadow:0 8px 24px rgba(0,0,0,.12)"></div>
        <% } else { %>
        <div style="display:flex;gap:12px;flex-wrap:wrap"><img src="/image/tp108.png" style="width:200px;border-radius:8px;box-shadow:0 8px 24px rgba(0,0,0,.12)"><img src="/image/tp109.png" style="width:200px;border-radius:8px;box-shadow:0 8px 24px rgba(0,0,0,.12)"></div>
        <% } %>
        <% if (product.id != 205) { %>
        <div class="price-notice" style="margin-top:16px;color:#6c7a89;font-size:13px;line-height:1.8">
          <div style="font-weight:700;color:#4a4a4a;margin-bottom:8px">价格说明：</div>
          <div style="font-weight:700;color:#4a4a4a;margin-bottom:4px">被比较价格</div>
          <p>商品的专柜价、吊牌价、厂家建议零售价、厂商指导价、品牌供应商提供的指导价、商家在后台设置的一口价、减价（折价）前的基准价或该商品曾经展示过的销售价等。该价格仅供您在购买时参考。</p>
          <div style="font-weight:700;color:#4a4a4a;margin:10px 0 4px">标价/售价</div>
          <p>商品的实时标价。具体成交价格根据商品参加活动，或会员使用优惠券、积分等发生变化，最终以订单结算页价格为准。</p>
        </div>
        <% } %>
      </div>
      <div class="tab-panel">
        <div style="display:flex;gap:12px;flex-wrap:wrap">
          <a href="/product/202" style="display:flex;align-items:center;gap:8px;padding:8px 12px;border:1px solid #eee;border-radius:8px;text-decoration:none;color:#333"><img src="/image/tp84.png" style="width:48px;height:48px;border-radius:6px"><span>苹果手机壳</span></a>
          <a href="/product/203" style="display:flex;align-items:center;gap:8px;padding:8px 12px;border:1px solid #eee;border-radius:8px;text-decoration:none;color:#333"><img src="/image/tp85.png" style="width:48px;height:48px;border-radius:6px"><span>华为手机壳</span></a>
          <a href="/products/all?keyword=%E6%89%8B%E6%9C%BA%E5%A3%B3&tag=accessory" style="display:flex;align-items:center;gap:8px;padding:8px 12px;border:1px solid #eee;border-radius:8px;text-decoration:none;color:#333"><img src="/image/tp86.png" style="width:48px;height:48px;border-radius:6px"><span>更多手机壳</span></a>
        </div>
      </div>
    </div>
  </div>

  <!-- footer -->

  <script>
    // 显示购物车通知弹窗
    function showCartNotification() {
      const overlay = document.getElementById('overlay');
      const notification = document.getElementById('cartNotification');

      overlay.classList.add('show');
      notification.classList.add('show');

      // 3秒后自动关闭
      setTimeout(() => {
        closeNotification();
      }, 3000);
    }

    // 关闭通知弹窗
    function closeNotification() {
      const overlay = document.getElementById('overlay');
      const notification = document.getElementById('cartNotification');

      overlay.classList.remove('show');
      notification.classList.remove('show');
    }

    // 显示错误通知
    function showErrorNotification(message) {
      const notification = document.getElementById('cartNotification');
      const icon = notification.querySelector('.icon');
      const title = notification.querySelector('h3');
      const text = notification.querySelector('p');

      // 修改为错误样式
      icon.innerHTML = '✕';
      icon.style.background = 'linear-gradient(135deg, #f44336, #d32f2f)';
      title.textContent = '添加失败';
      text.textContent = message;

      showCartNotification();

      // 恢复原样式
      setTimeout(() => {
        icon.innerHTML = '✓';
        icon.style.background = 'linear-gradient(135deg, #4CAF50, #45a049)';
        title.textContent = '添加成功！';
        text.textContent = '商品已成功添加到购物车';
      }, 3000);
    }

    // 添加商品到购物车的函数
    async function addToCart(productId, quantity = 1) {
      const button = document.querySelector('.add-to-cart-btn');
      const spinner = document.querySelector('.loading-spinner');

      try {
        // 添加点击动画效果
        button.classList.add('clicked');
        button.classList.add('loading');

        // 移除点击动画
        setTimeout(() => {
          button.classList.remove('clicked');
        }, 600);

        const response = await fetch('/cart/add', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({
            productId: productId,
            quantity: quantity
          })
        });

        button.classList.remove('loading');

        if (response.ok) {
          showCartNotification();
        } else if (response.status === 401) {
          showErrorNotification('请先登录后再添加商品到购物车');
          setTimeout(() => {
            window.location.href = '/login.html';
          }, 2000);
        } else {
          const error = await response.text();
          showErrorNotification('添加到购物车失败：' + error);
        }
      } catch (error) {
        button.classList.remove('loading');
        console.error('添加到购物车时发生错误:', error);
        showErrorNotification('网络错误，请稍后重试');
      }
    }

    // 页面加载完成后添加事件监听器
    document.addEventListener('DOMContentLoaded', function () {
      const addToCartButton = document.querySelector('.add-to-cart-btn');
      const overlay = document.getElementById('overlay');

      if (addToCartButton) {
        addToCartButton.addEventListener('click', function () {
          const productId = this.getAttribute('data-product-id');
          const qtyInput = document.getElementById('buyQty');
          const qty = qtyInput ? Math.max(1, parseInt(qtyInput.value || '1', 10)) : 1;
          if (productId) {
            addToCart(productId, qty);
          }
        });
      }
      const buyNowButton = document.querySelector('.buy-btn');
      if (buyNowButton) {
        buyNowButton.addEventListener('click', async function () {
          const productId = this.getAttribute('data-product-id');
          const qtyInput = document.getElementById('buyQty');
          const qty = qtyInput ? Math.max(1, parseInt(qtyInput.value || '1', 10)) : 1;
          try {
            await addToCart(productId, qty);
            window.location.href = '/cart';
          } catch (e) {}
        });
      }
      const qtyInput = document.getElementById('buyQty');
      const minusBtn = document.querySelector('.qty-btn.minus');
      const plusBtn = document.querySelector('.qty-btn.plus');
      if (qtyInput && minusBtn && plusBtn) {
        minusBtn.addEventListener('click', function () {
          qtyInput.value = Math.max(1, parseInt(qtyInput.value || '1', 10) - 1);
        });
        plusBtn.addEventListener('click', function () {
          qtyInput.value = Math.max(1, parseInt(qtyInput.value || '1', 10) + 1);
        });
      }
      const thumb = document.querySelector('.gallery .thumbs img');
      const main = document.getElementById('mainImage');
      if (thumb && main) {
        thumb.addEventListener('click', function(){ main.src = this.src; });
      }

      // 点击遮罩层关闭弹窗
      if (overlay) {
        overlay.addEventListener('click', closeNotification);
      }

      // ESC键关闭弹窗
      document.addEventListener('keydown', function (e) {
        if (e.key === 'Escape') {
          closeNotification();
        }
      });

      const mainPic = document.querySelector('.gallery .main-pic');
      const mainImg = document.getElementById('mainImage');
      if (mainPic && mainImg) {
        const lens = document.createElement('div');
        lens.className = 'zoom-lens';
        mainPic.appendChild(lens);
        const overlay = document.createElement('div');
        overlay.id = 'zoomOverlay';
        overlay.className = 'zoom-overlay';
        document.body.appendChild(overlay);
        const zoom = 2.2;
        lens.style.left = '0px';
        lens.style.top = '0px';
        let picRect, imgRect, minX, minY, maxX, maxY, lensW, lensH, overlayW, overlayH;
        const placeOverlay = () => {
          const rect = mainPic.getBoundingClientRect();
          const margin = 20;
          let left = rect.right + margin;
          if (left + overlay.offsetWidth > window.innerWidth - 12) left = rect.left - overlay.offsetWidth - margin;
          let top = rect.top;
          if (top + overlay.offsetHeight > window.innerHeight - 12) top = Math.max(12, window.innerHeight - overlay.offsetHeight - 12);
          overlay.style.left = left + 'px';
          overlay.style.top = top + 'px';
        };
        const setBg = () => {
          if (!mainImg.src) return;
          imgRect = mainImg.getBoundingClientRect();
          overlay.style.backgroundImage = 'url(' + mainImg.src + ')';
          overlay.style.backgroundSize = (imgRect.width * zoom) + 'px ' + (imgRect.height * zoom) + 'px';
          picRect = mainPic.getBoundingClientRect();
          lensW = lens.offsetWidth; lensH = lens.offsetHeight; overlayW = overlay.clientWidth; overlayH = overlay.clientHeight;
          minX = imgRect.left - picRect.left;
          minY = imgRect.top - picRect.top;
          maxX = minX + imgRect.width - lensW;
          maxY = minY + imgRect.height - lensH;
        };
        setBg();
        placeOverlay();
        mainImg.addEventListener('load', () => { setBg(); placeOverlay(); });
        window.addEventListener('resize', () => { setBg(); placeOverlay(); });
        let targetX = minX || 0, targetY = minY || 0;
        let running = false;
        const render = () => {
          lens.style.transform = 'translate3d(' + targetX + 'px,' + targetY + 'px,0)';
          const cxRel = (targetX - minX) + lensW / 2;
          const cyRel = (targetY - minY) + lensH / 2;
          const bgX = cxRel * zoom - overlayW / 2;
          const bgY = cyRel * zoom - overlayH / 2;
          overlay.style.backgroundPosition = '-' + bgX + 'px -' + bgY + 'px';
        };
        const loop = () => { if (!running) return; render(); requestAnimationFrame(loop); };
        const move = (e) => {
          const clientX = e.touches ? e.touches[0].clientX : e.clientX;
          const clientY = e.touches ? e.touches[0].clientY : e.clientY;
          let x = clientX - picRect.left - lensW / 2;
          let y = clientY - picRect.top - lensH / 2;
          if (x < minX) x = minX;
          if (y < minY) y = minY;
          if (x > maxX) x = maxX;
          if (y > maxY) y = maxY;
          targetX = x; targetY = y;
        };
        const show = () => { lens.classList.add('show'); overlay.classList.add('show'); placeOverlay(); };
        const hide = () => { lens.classList.remove('show'); overlay.classList.remove('show'); running = false; };
        mainPic.addEventListener('mousemove', move, { passive: true });
        mainPic.addEventListener('mouseenter', show);
        mainPic.addEventListener('mouseleave', hide);
        mainPic.addEventListener('touchmove', move, { passive: true });
        mainPic.addEventListener('touchstart', show, { passive: true });
        mainPic.addEventListener('touchend', hide);
        mainPic.addEventListener('mouseenter', () => { running = true; requestAnimationFrame(loop); });
        mainPic.addEventListener('touchstart', () => { running = true; requestAnimationFrame(loop); }, { passive: true });
      }
      const tabs = document.querySelectorAll('.image-tabs .tabs-nav .tab');
      const panels = document.querySelectorAll('.image-tabs .tab-panel');
      if (tabs.length && panels.length) {
        tabs.forEach(function(tab, idx){
          tab.addEventListener('click', function(){
            tabs.forEach(function(t){ t.classList.remove('active'); });
            panels.forEach(function(p){ p.classList.remove('active'); });
            tab.classList.add('active');
            panels[idx].classList.add('active');
          });
        });
      }
    });
  </script>
</body>

</html>
